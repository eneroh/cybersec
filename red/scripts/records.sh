#!/bin/bash

=== records ===

--- python shell to tty shell - pty module ---
python3 -c 'import pty; pty.spawn("/bin/bash")'

Priv esc docs

Ways to improve a terminal through reverse shell & priv esc etc.

Will place python bin bash command here as well
